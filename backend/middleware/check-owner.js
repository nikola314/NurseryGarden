module.exports = (req, res, next) => {
    // if (req.userData.accountType == false) {
    //     //is not admin
    //     res.status(401).json({ message: "You are not admin!" });
    // } else {
    next();
    // }

    // TODO: check if userid == product.manufacturer
};